// Flow planner — the site. Two modes: Plan (2D drafting) and Go live (3D).
// STUB until the prototype is staged; the port fills:
//   plan/  — SVG drafting surface (praxis-plan.js)
//   live/  — three.js model (praxis-3d.js)
export function FlowPlanner() {
  return (
    <section style={{ padding: 24 }}>
      <h1 style={{ margin: 0, fontSize: 18 }}>Flow planner</h1>
      <p style={{ color: 'var(--ink-3)' }}>Porting from the prototype…</p>
    </section>
  )
}

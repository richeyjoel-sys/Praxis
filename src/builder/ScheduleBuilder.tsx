// Schedule builder — shifts and movements. Where every planning decision is made.
// STUB until the prototype is staged; the port fills this directory with:
//   ShiftCard, RoleRow, MovementRow, cascade/ (six steps), AddMovement, Captains.
export function ScheduleBuilder() {
  return (
    <section style={{ padding: 24 }}>
      <h1 style={{ margin: 0, fontSize: 18 }}>Schedule builder</h1>
      <p style={{ color: 'var(--ink-3)' }}>Porting from the prototype…</p>
    </section>
  )
}
